<!--
WildRobot v2 (full body with arms) - placeholder skeleton.

This file is intentionally minimal so the repo can carry an "assets/v2" layout
before the Onshape export is wired in.

Next steps:
- Export MJCF from Onshape assembly "WildRobot" into this folder
- Ensure meshes land in assets/v2/assets/
- Run:
    cd assets/v2 && uv run python ../post_process.py wildrobot.xml
-->
<mujoco model="WildRobot">
  <compiler angle="radian" meshdir="assets" autolimits="true" />
  <option timestep="0.002" />

  <worldbody>
    <!-- TODO: replace with exported full-body model -->
    <body name="waist" pos="0 0 0.5">
      <freejoint name="waist_freejoint" />
      <geom type="sphere" size="0.05" rgba="0.8 0.2 0.2 0.7" />
    </body>
  </worldbody>
</mujoco>

