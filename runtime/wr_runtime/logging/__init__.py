"""Shared runtime/training logging contracts for locomotion.

The previous ``locomotion_log_schema`` module (v0.19.x runtime FSM
log contract) was deleted at v0.20.1 along with the v1/v2 walking
references it served.  A v3-aligned log schema will land with the
hardware-deployable runtime adapter (v0.20.2 milestone).
"""
